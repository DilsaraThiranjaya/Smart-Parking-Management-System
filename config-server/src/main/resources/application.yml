server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DilsaraThiranjaya/SPMS-cloud-config.git
          clone-on-start: true
          search-paths: '{application}'